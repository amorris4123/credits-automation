# Core Dependencies
slack-sdk>=3.23.0
python-dotenv>=1.0.0
requests>=2.31.0

# Notebook Execution
papermill>=2.5.0
nbformat>=5.9.0
ipykernel>=6.25.0

# Data Processing (if needed by notebook)
pandas>=2.1.0
numpy>=1.24.0

# Looker API (if specific library exists, otherwise requests is sufficient)
# looker-sdk>=23.0.0  # Optional - using requests for now

# Logging and Utilities
coloredlogs>=15.0  # Optional: colored console logs
